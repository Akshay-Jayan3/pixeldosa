{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "thinking-experience",
  "title": "Thinking Experience",
  "description": "A complete agent run, orchestrated — the first Level 2 Block in this system, and the composed surface the AI-tier components exist to be assembled into. Takes one `state` from the agent vocabulary plus a `status` line, optional `reasoning` steps, an optional `ask` request, an optional `approval` request, and a terminal `result` with its action strip, then decides which of those surfaces belongs on screen at each moment. The value over rendering the same components yourself is entirely the orchestration: a consumer wiring Live Status Line, Reasoning Stream, Agent Ask and AI Approval Gate together has to re-derive when each appears, when reasoning should stop streaming, when the elapsed counter should freeze, and what happens to the machine's own surfaces while a human is being asked something — and that last question is the one most implementations get wrong. This block applies the turn-taking grammar at the level of the whole panel rather than a single indicator: when control passes back to the user, the machine's status and reasoning visibly step down in emphasis and whatever needs an answer takes a ring, while remaining fully interactive so a run can always be stopped even while it waits on you. Composes Live Status Line, Reasoning Stream, Agent Ask, AI Approval Gate and AI Action Toolbar, which transitively pulls in Agent Presence, Confidence Meter and AI Context Surface. Use it as the agent panel in a product — a sidebar, a drawer, an inline run surface. Not a chat thread and not a transcript: it shows one run in progress, not a history of turns.",
  "dependencies": [],
  "registryDependencies": [
    "@pixeldosa/pixeldosa-theme",
    "@pixeldosa/live-status-line",
    "@pixeldosa/reasoning-stream",
    "@pixeldosa/agent-ask",
    "@pixeldosa/ai-approval-gate",
    "@pixeldosa/ai-action-toolbar"
  ],
  "files": [
    {
      "path": "../../packages/ui/src/registry/thinking-experience/thinking-experience.tsx",
      "content": "\"use client\";\n\nimport * as React from \"react\";\n\nimport { AgentAsk, type AskField } from \"@/components/ui/agent-ask\";\nimport type { AgentState } from \"@/components/ui/agent-presence\";\nimport { AIActionToolbar, type AIAction } from \"@/components/ui/ai-action-toolbar\";\nimport {\n  AIApprovalGate,\n  type ApprovalImpact,\n  type ApprovalRisk,\n} from \"@/components/ui/ai-approval-gate\";\nimport type { ProvenanceSource } from \"@/components/ui/ai-context-surface\";\nimport type { ConfidenceTier } from \"@/components/ui/confidence-meter\";\nimport { LiveStatusLine } from \"@/components/ui/live-status-line\";\nimport { ReasoningStream } from \"@/components/ui/reasoning-stream\";\nimport { cn } from \"@/lib/utils\";\n\n/** A pending elicitation — the agent cannot continue without an answer. */\nexport type AskRequest = {\n  source: string;\n  question: string;\n  explanation?: string;\n  fields: AskField[];\n  onRespond: (values: Record<string, string | boolean>) => void;\n  onDecline?: () => void;\n  onCancel?: () => void;\n};\n\n/** A pending authorisation — the agent wants to do something consequential. */\nexport type ApprovalRequest = {\n  action: string;\n  reversible: boolean;\n  risk?: ApprovalRisk;\n  impact?: ApprovalImpact[];\n  confidence?: ConfidenceTier;\n  provenance?: string;\n  explanation?: string;\n  sources?: ProvenanceSource[];\n  onApprove: () => void;\n  onReject: () => void;\n  onEdit?: () => void;\n  busy?: \"approve\" | \"reject\" | null;\n};\n\nexport interface ThinkingExperienceProps extends React.ComponentPropsWithoutRef<\"section\"> {\n  state: AgentState;\n  status: string;\n  detail?: string;\n  startedAt?: number | Date;\n  reasoning?: string[];\n  reasoningMs?: number;\n  /** Rendered when `state` is `asking`. */\n  ask?: AskRequest;\n  /** Rendered when `state` is `awaitingApproval`. */\n  approval?: ApprovalRequest;\n  /** The finished output. Rendered once the run reaches a terminal state. */\n  result?: React.ReactNode;\n  resultActions?: AIAction[];\n  onResultAction?: (actionId: string) => void;\n  onCancel?: () => void;\n}\n\nconst USER_TURN: AgentState[] = [\"asking\", \"awaitingApproval\", \"suggesting\"];\nconst TERMINAL: AgentState[] = [\"done\", \"failed\", \"cancelled\"];\n\n/**\n * A complete agent run, orchestrated — the composed surface the individual AI-tier\n * components exist to be assembled into.\n *\n * The value it adds over rendering those components yourself is the orchestration:\n * knowing which surface belongs on screen at which moment, and applying the\n * turn-taking grammar at the level of the whole panel rather than a single indicator.\n * When the agent hands control back, the machine's own surfaces visibly step down and\n * the thing needing an answer takes the emphasis — which is the same rule as\n * DESIGN.md §3a, scaled from one dot to an entire layout.\n */\nfunction ThinkingExperience({\n  state,\n  status,\n  detail,\n  startedAt,\n  reasoning,\n  reasoningMs,\n  ask,\n  approval,\n  result,\n  resultActions,\n  onResultAction,\n  onCancel,\n  className,\n  ...props\n}: ThinkingExperienceProps) {\n  const userTurn = USER_TURN.includes(state);\n  const terminal = TERMINAL.includes(state);\n  const interrupt = (state === \"asking\" && ask) || (state === \"awaitingApproval\" && approval);\n\n  return (\n    <section\n      aria-label=\"Agent run\"\n      className={cn(\"flex flex-col gap-3 rounded-lg border bg-card p-3\", className)}\n      {...props}\n    >\n      {/* The machine's own account of itself. It steps down — but is never disabled —\n          when control passes back: you must always be able to stop a run, including\n          one that is waiting on you.\n          The step-down used to be `opacity-55` on the whole region, which took its text\n          below readable contrast (2.1:1 in light mode). Emphasis now drops by role\n          instead: decorative graphics fade, and primary text falls to the muted\n          colour, which still clears 4.5:1. */}\n      <div\n        className={cn(\n          \"flex flex-col gap-2 [&_*]:transition-[color,opacity] [&_*]:duration-[var(--pd-duration-base)] [&_*]:ease-[var(--pd-ease-standard)] motion-reduce:[&_*]:transition-none\",\n          interrupt && \"[&_[data-pd-decorative]]:opacity-40 [&_svg[aria-hidden=true]]:opacity-40 [&_.text-foreground]:text-muted-foreground\"\n        )}\n      >\n        <LiveStatusLine\n          state={state}\n          status={status}\n          detail={detail}\n          startedAt={terminal ? undefined : startedAt}\n          onCancel={onCancel}\n        />\n\n        {reasoning && reasoning.length > 0 ? (\n          <div className=\"pl-[22px]\">\n            <ReasoningStream\n              steps={reasoning}\n              isStreaming={!terminal && !userTurn}\n              durationMs={reasoningMs}\n            />\n          </div>\n        ) : null}\n      </div>\n\n      {/* Whatever needs an answer. Emphasised by a ring rather than by motion, because\n          this is the user's turn and stillness is the signal. */}\n      {interrupt ? (\n        <div className=\"rounded-lg ring-2 ring-ring/30\">\n          {state === \"asking\" && ask ? (\n            <AgentAsk\n              source={ask.source}\n              question={ask.question}\n              explanation={ask.explanation}\n              fields={ask.fields}\n              onRespond={ask.onRespond}\n              onDecline={ask.onDecline}\n              onCancel={ask.onCancel}\n              className=\"border-0\"\n            />\n          ) : null}\n\n          {state === \"awaitingApproval\" && approval ? (\n            <AIApprovalGate\n              action={approval.action}\n              risk={approval.risk}\n              reversible={approval.reversible}\n              impact={approval.impact}\n              confidence={approval.confidence}\n              provenance={approval.provenance}\n              explanation={approval.explanation}\n              sources={approval.sources}\n              busy={approval.busy}\n              onApprove={approval.onApprove}\n              onReject={approval.onReject}\n              onEdit={approval.onEdit}\n              className=\"border-0\"\n            />\n          ) : null}\n        </div>\n      ) : null}\n\n      {terminal && result ? (\n        <div className=\"flex flex-col gap-3 border-t pt-3\">\n          <div className=\"animate-[pd-fade-in_var(--pd-duration-base)_var(--pd-ease-decelerate)_both] motion-reduce:animate-none\">\n            {result}\n          </div>\n          {resultActions && resultActions.length > 0 ? (\n            <AIActionToolbar\n              label=\"Result actions\"\n              actions={resultActions}\n              onAction={(id) => onResultAction?.(id)}\n            />\n          ) : null}\n        </div>\n      ) : null}\n    </section>\n  );\n}\n\nexport { ThinkingExperience };\n",
      "type": "registry:block",
      "target": "components/blocks/thinking-experience.tsx"
    }
  ],
  "meta": {
    "engineeringNotes": "The first Block in this system, and it is deliberately an orchestration rather than new UI: every visible element comes from an already-shipped component. What it contributes is the set of decisions a consumer would otherwise have to re-derive — which surface belongs on screen at which state, that reasoning stops streaming the moment control passes to a human rather than continuing to tick behind a question, that the elapsed counter freezes on terminal states instead of counting forever, and that a pending question or approval is what the eye should land on. The interesting decision is how the turn-taking grammar scales: at component level it is one indicator moving or holding still, but at panel level it becomes relative emphasis, so the machine's status and reasoning step down (indicator graphics fade to 40% and primary text drops to the muted colour, keeping text above 4.5:1 contrast rather than dimming it with opacity) while an interrupt takes a ring. They are dimmed but deliberately never disabled — a run must remain stoppable even while it is waiting on the person, and `pointer-events-none` on that region would have silently removed the Stop control at exactly the moment someone might want it. Emphasis is carried by a ring rather than by animation because this is the user's turn and stillness is the signal; animating the thing that wants attention would contradict the grammar the rest of the system holds. Ask and approval are passed as plain data objects rather than as slots so the block owns the decision of when each appears — a slot-based API would hand the orchestration question straight back to the consumer, which is the one thing this component exists to answer.",
    "motionNotes": "Two transitions, both mapped to a real state change. The machine region cross-fades its text colour and indicator opacity over `--pd-duration-base` with `--pd-ease-standard` as control passes between the agent and the user, which is the panel-level expression of the turn-taking grammar and the only motion that belongs to this block itself. The result fades in once over `--pd-duration-base` with `--pd-ease-decelerate` when the run reaches a terminal state. Everything else is inherited: Live Status Line's status swap, Agent Presence's rotation, Reasoning Stream's step fades and disclosure, AI Action Toolbar's colour transitions and busy spinner. Nothing is duplicated at this level, which is the point of composing rather than reimplementing — one rhythm, set once in the tokens, applied consistently by every part. Under `prefers-reduced-motion` the block's own two transitions drop to instant and each child handles its own fallback, leaving the layout, the ring emphasis and the opacity difference fully legible without any movement."
  },
  "docs": "Requires `cn` at `@/lib/utils`. Installing this pulls in the whole AI-tier stack it composes — Live Status Line, Reasoning Stream, Agent Ask, AI Approval Gate and AI Action Toolbar, plus Agent Presence, Confidence Meter and AI Context Surface transitively. Drive it from a single agent state; passing `ask` and `approval` alongside is harmless, since only the one matching the current state renders.",
  "categories": [
    "ai-assisted",
    "application",
    "block"
  ],
  "type": "registry:block"
}
