{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "command-menu",
  "title": "Command Menu",
  "description": "A global, keyboard-first overlay (⌘K/Ctrl+K) for finding resources and running actions across an app. Renders as a centered floating panel on desktop and a full-width sheet under the sm breakpoint. Exports CommandMenu (the controlled root), CommandMenuInput, CommandMenuList (which auto-renders a live result-count announcer), CommandMenuEmpty, CommandMenuGroup, CommandMenuSeparator, and CommandMenuItem (accepts onSelect, an optional leading icon, and an optional keybind hint rendered as a kbd badge). Composes cmdk for the ARIA combobox/filter engine under a Radix Dialog for focus trapping, portalling and dismiss-on-escape/outside-click, with a token-driven CSS fade+scale for entrance/exit. Controlled-only: there is no internal open state, because the global shortcut listener has to live in the host app regardless. Use for apps with enough destinations or repeat actions that menu-hunting has real cost — dashboards, admin panels, developer tools; do not use it for a menu triggered from one visible button or row, use a Menu or ContextMenu instead.",
  "dependencies": [
    "cmdk",
    "@radix-ui/react-dialog"
  ],
  "registryDependencies": [
    "@pixeldosa/pixeldosa-theme"
  ],
  "files": [
    {
      "path": "../../packages/ui/src/registry/command-menu/command-menu.tsx",
      "content": "\"use client\";\n\nimport * as React from \"react\";\nimport * as Dialog from \"@radix-ui/react-dialog\";\nimport { Command as CommandPrimitive, useCommandState } from \"cmdk\";\n\nimport { cn } from \"@/lib/utils\";\n\nexport interface CommandMenuProps\n  extends Omit<React.ComponentPropsWithoutRef<typeof CommandPrimitive>, \"onSelect\"> {\n  /** Controlled open state. There is no uncontrolled fallback — see engineeringNotes. */\n  open: boolean;\n  onOpenChange: (open: boolean) => void;\n  /** Accessible dialog name. Not visible; announced once when the menu opens. */\n  label?: string;\n}\n\n/**\n * Keeps the panel mounted for `exitMs` after `open` goes false, so the CSS exit\n * transition has time to play before the element actually leaves the DOM.\n *\n * Deliberately does not use the shared `overlay` primitive's Motion-driven\n * enter/exit here: `AnimatePresence` has a confirmed upstream bug\n * (motiondivision/motion#3243) where, under certain timing, it never detects that\n * an exiting child's animation finished, so the element (and, worse, its\n * full-viewport scrim) never actually unmounts — silently blocking all further\n * page interaction after the first close. That reproduces in the existing Overlay\n * demo too, in both dev and production builds, so it is not specific to this\n * component. A plain CSS transition driven by a deterministic timer sidesteps the\n * bug entirely; see engineeringNotes for the follow-up to reconcile this with the\n * shared primitive once it is fixed at the source.\n */\nfunction useDelayedUnmount(open: boolean, exitMs: number) {\n  const [mounted, setMounted] = React.useState(open);\n  const [visible, setVisible] = React.useState(open);\n\n  React.useEffect(() => {\n    if (open) {\n      setMounted(true);\n      // Mount in the closed visual state first, then flip to open on the next\n      // frame — changing both in the same commit would skip the CSS transition\n      // entirely, since there'd be no \"from\" state for the browser to animate.\n      const raf = requestAnimationFrame(() => setVisible(true));\n      return () => cancelAnimationFrame(raf);\n    }\n    setVisible(false);\n    const timeout = window.setTimeout(() => setMounted(false), exitMs);\n    return () => window.clearTimeout(timeout);\n  }, [open, exitMs]);\n\n  return { mounted, visible };\n}\n\n/**\n * A global, keyboard-first overlay for finding resources and running actions across an\n * app. Composes cmdk for the ARIA combobox contract (input, listbox, roving\n * aria-activedescendant, filtering) under a Radix `Dialog` for focus trapping,\n * portalling and dismiss behaviour, with a fade+scale entrance/exit matching the rest\n * of the system's overlay language.\n *\n * Controlled only: mount `<CommandMenu open onOpenChange>` and toggle `open` from\n * whatever global `⌘K`/`Ctrl+K` listener your app already owns.\n */\nconst CommandMenu = React.forwardRef<HTMLDivElement, CommandMenuProps>(function CommandMenu(\n  { open, onOpenChange, label = \"Command Menu\", className, children, filter, shouldFilter, loop = true, ...props },\n  ref\n) {\n  const exitMs = 150;\n  const { mounted, visible } = useDelayedUnmount(open, exitMs);\n\n  return (\n    <Dialog.Root open={open} onOpenChange={onOpenChange}>\n      {mounted ? (\n        <Dialog.Portal forceMount>\n          <Dialog.Overlay asChild forceMount>\n            <div\n              data-state={visible ? \"open\" : \"closed\"}\n              className={cn(\n                \"fixed inset-0 z-50 bg-background/85\",\n                \"opacity-0 transition-opacity duration-[var(--pd-duration-fast)] ease-[var(--pd-ease-standard)]\",\n                \"data-[state=open]:opacity-100\",\n                \"motion-reduce:transition-none\"\n              )}\n            />\n          </Dialog.Overlay>\n          <Dialog.Content\n            asChild\n            forceMount\n            onOpenAutoFocus={(event) => event.preventDefault()}\n          >\n            <div\n              data-state={visible ? \"open\" : \"closed\"}\n              className={cn(\n                \"fixed inset-x-3 bottom-3 z-50 flex max-h-[70vh] flex-col overflow-hidden rounded-lg border bg-popover p-0 text-popover-foreground shadow-lg\",\n                \"sm:inset-x-auto sm:bottom-auto sm:left-1/2 sm:top-1/2 sm:w-full sm:max-w-lg sm:-translate-x-1/2 sm:-translate-y-1/2\",\n                \"opacity-0 scale-[0.98] transition-[opacity,scale] duration-[var(--pd-duration-fast)] ease-[var(--pd-ease-standard)]\",\n                \"data-[state=open]:opacity-100 data-[state=open]:scale-100\",\n                \"motion-reduce:transition-none motion-reduce:scale-100\",\n                className\n              )}\n            >\n              <Dialog.Title className=\"sr-only\">{label}</Dialog.Title>\n              <Dialog.Description className=\"sr-only\">\n                Type to search, use the arrow keys to move, Enter to select, Escape to\n                close.\n              </Dialog.Description>\n              <CommandPrimitive\n                ref={ref}\n                label={label}\n                filter={filter}\n                shouldFilter={shouldFilter}\n                loop={loop}\n                className=\"flex min-h-0 flex-1 flex-col\"\n                onKeyDown={(event) => {\n                  // Belt-and-suspenders: cmdk's own keydown handling on this element\n                  // stops Escape from reaching Radix's document-level dismissable-layer\n                  // listener, so Escape-to-close silently no-ops when cmdk is composed\n                  // manually inside Dialog.Content instead of via its own\n                  // Command.Dialog wrapper. Handling it explicitly here is more\n                  // reliable than fighting cmdk's internal event handling.\n                  if (event.key === \"Escape\") {\n                    onOpenChange(false);\n                  }\n                }}\n                {...props}\n              >\n                {children}\n              </CommandPrimitive>\n            </div>\n          </Dialog.Content>\n        </Dialog.Portal>\n      ) : null}\n    </Dialog.Root>\n  );\n});\n\nexport interface CommandMenuInputProps\n  extends React.ComponentPropsWithoutRef<typeof CommandPrimitive.Input> {}\n\nconst CommandMenuInput = React.forwardRef<HTMLInputElement, CommandMenuInputProps>(\n  function CommandMenuInput({ className, ...props }, ref) {\n    return (\n      <div className=\"flex shrink-0 items-center border-b px-3\">\n        <CommandPrimitive.Input\n          ref={ref}\n          autoFocus\n          className={cn(\n            \"flex h-11 w-full bg-transparent text-sm text-foreground outline-none\",\n            \"placeholder:text-muted-foreground disabled:cursor-not-allowed disabled:opacity-50\",\n            className\n          )}\n          {...props}\n        />\n      </div>\n    );\n  }\n);\n\n/**\n * `aria-live=\"polite\"` region announcing the filtered result count on every query\n * change — required so screen-reader users notice a silent count change, per the\n * Vercel command-menu spec this component's plan was built against. Rendered\n * automatically by `CommandMenuList` rather than left for consumers to wire up.\n */\nfunction CommandMenuResultAnnouncer() {\n  const count = useCommandState((state) => state.filtered.count);\n  return (\n    <span role=\"status\" aria-live=\"polite\" className=\"sr-only\">\n      {count} {count === 1 ? \"result\" : \"results\"}\n    </span>\n  );\n}\n\nexport interface CommandMenuListProps\n  extends React.ComponentPropsWithoutRef<typeof CommandPrimitive.List> {}\n\nconst CommandMenuList = React.forwardRef<HTMLDivElement, CommandMenuListProps>(\n  function CommandMenuList({ className, children, ...props }, ref) {\n    return (\n      <>\n        <CommandMenuResultAnnouncer />\n        <CommandPrimitive.List\n          ref={ref}\n          className={cn(\"min-h-0 flex-1 overflow-y-auto overscroll-contain p-1.5\", className)}\n          {...props}\n        >\n          {children}\n        </CommandPrimitive.List>\n      </>\n    );\n  }\n);\n\nexport interface CommandMenuEmptyProps\n  extends React.ComponentPropsWithoutRef<typeof CommandPrimitive.Empty> {}\n\nconst CommandMenuEmpty = React.forwardRef<HTMLDivElement, CommandMenuEmptyProps>(\n  function CommandMenuEmpty({ className, ...props }, ref) {\n    return (\n      <CommandPrimitive.Empty\n        ref={ref}\n        className={cn(\"py-8 text-center text-sm text-muted-foreground\", className)}\n        {...props}\n      />\n    );\n  }\n);\n\nexport interface CommandMenuGroupProps\n  extends React.ComponentPropsWithoutRef<typeof CommandPrimitive.Group> {}\n\nconst CommandMenuGroup = React.forwardRef<HTMLDivElement, CommandMenuGroupProps>(\n  function CommandMenuGroup({ className, ...props }, ref) {\n    return (\n      <CommandPrimitive.Group\n        ref={ref}\n        className={cn(\n          \"overflow-hidden py-1.5\",\n          \"[&_[cmdk-group-heading]]:px-2 [&_[cmdk-group-heading]]:pb-1.5\",\n          \"[&_[cmdk-group-heading]]:text-xs [&_[cmdk-group-heading]]:font-medium\",\n          \"[&_[cmdk-group-heading]]:text-muted-foreground\",\n          className\n        )}\n        {...props}\n      />\n    );\n  }\n);\n\nconst CommandMenuSeparator = React.forwardRef<\n  HTMLDivElement,\n  React.ComponentPropsWithoutRef<typeof CommandPrimitive.Separator>\n>(function CommandMenuSeparator({ className, ...props }, ref) {\n  return (\n    <CommandPrimitive.Separator\n      ref={ref}\n      className={cn(\"-mx-1.5 my-1.5 h-px bg-border\", className)}\n      {...props}\n    />\n  );\n});\n\nexport interface CommandMenuItemProps\n  extends Omit<React.ComponentPropsWithoutRef<typeof CommandPrimitive.Item>, \"onSelect\"> {\n  onSelect: (value: string) => void;\n  /** Leading icon, muted by default so it recedes behind the label. size-4 recommended. */\n  icon?: React.ReactNode;\n  /**\n   * Rendered visibly in the row and, because it stays in the item's text flow rather\n   * than an aria-hidden decoration, announced as part of the option's accessible name —\n   * satisfying \"hotkeys are both visible and announced\" without extra ARIA wiring.\n   */\n  keybind?: string;\n}\n\nconst CommandMenuItem = React.forwardRef<HTMLDivElement, CommandMenuItemProps>(\n  function CommandMenuItem({ className, icon, keybind, children, ...props }, ref) {\n    return (\n      <CommandPrimitive.Item\n        ref={ref}\n        className={cn(\n          \"relative flex cursor-default select-none items-center gap-2 rounded-md px-2 py-1.5 text-sm\",\n          \"text-foreground outline-none\",\n          \"data-[selected=true]:bg-accent data-[selected=true]:text-accent-foreground\",\n          \"data-[disabled=true]:pointer-events-none data-[disabled=true]:opacity-50\",\n          \"transition-colors duration-[var(--pd-duration-instant)] ease-[var(--pd-ease-standard)]\",\n          \"motion-reduce:transition-none\",\n          className\n        )}\n        {...props}\n      >\n        {icon ? (\n          <span className=\"flex size-4 shrink-0 items-center justify-center text-muted-foreground [&_svg]:size-4\">\n            {icon}\n          </span>\n        ) : null}\n        <span className=\"flex-1 truncate\">{children}</span>\n        {keybind ? (\n          <kbd className=\"ml-auto shrink-0 rounded border bg-muted px-1.5 py-0.5 font-mono text-[10px] text-muted-foreground\">\n            {keybind}\n          </kbd>\n        ) : null}\n      </CommandPrimitive.Item>\n    );\n  }\n);\n\nexport {\n  CommandMenu,\n  CommandMenuInput,\n  CommandMenuList,\n  CommandMenuEmpty,\n  CommandMenuGroup,\n  CommandMenuSeparator,\n  CommandMenuItem,\n};\n",
      "type": "registry:ui",
      "target": "components/ui/command-menu.tsx"
    }
  ],
  "meta": {
    "engineeringNotes": "Uses cmdk for the input/list/filter engine rather than hand-rolling the ARIA combobox contract (input keeps DOM focus while aria-activedescendant points at the highlighted listbox option) — that contract is easy to get subtly wrong and cmdk is the same engine Vercel's own Geist command menu and shadcn/ui's command component build on. Radix Dialog wraps the whole thing rather than cmdk's own Command.Dialog variant, so PixelDosa keeps ownership of the visual contract instead of inheriting cmdk's default dialog chrome. Deliberately does NOT compose the shared `overlay` primitive (Motion-driven), despite that being the original plan: `overlay`'s AnimatePresence usage has a confirmed upstream bug (motiondivision/motion#3243) where the exiting panel and its full-viewport scrim sometimes never unmount, permanently blocking further page interaction after the first close — reproduced independently in Overlay's own existing demo, in both dev and production builds, so it is a primitive-level defect, not a composition mistake here. This component instead drives its fade+scale with a plain two-phase (mount in the closed state, flip to open on the next animation frame) CSS transition gated by a deterministic `setTimeout`-based delayed-unmount hook, reading the same `--pd-duration-fast`/`--pd-ease-standard` tokens `overlay` uses, so the visual result is identical while sidestepping the bug entirely. Recommend re-composing `overlay` here once its exit-completion issue is fixed upstream or worked around at the primitive level. Controlled-only open state (no internal fallback) because the consumer's global keydown listener already has to own the boolean. Also works around a second, unrelated composition gotcha: cmdk's own keydown handling on the Command root prevents Escape from reaching Radix's document-level dismissable-layer listener when cmdk is composed manually instead of via its own Command.Dialog, so Escape-to-close is handled explicitly rather than left to Radix alone. The result-count aria-live announcer is rendered automatically inside CommandMenuList via cmdk's useCommandState hook rather than left for every consumer to wire up. A separate kbd primitive was considered (per the component plan) and deliberately deferred: the keybind badge is a few lines of markup used only inside CommandMenuItem for v1, and extracting it into its own registry item before a second consumer needs it would be speculative.",
    "motionNotes": "Fade (opacity) plus a slight scale-in (0.98 → 1) on both the scrim and panel, matching the shared overlay primitive's visual signature even though it isn't composed directly here (see engineeringNotes). Mounts in the closed state, then flips to the open data-state on the next animation frame so the browser actually has a 'from' value to transition from — setting both in the same commit would skip the transition entirely. Duration and easing come from --pd-duration-fast/--pd-ease-standard, the same tokens Overlay uses, so the timing stays in step with the rest of the system. Exit is handled by a matching deterministic delay (150ms, comfortably above the fast token) before the panel actually leaves the DOM, so the fade-out is never cut short. The mobile sheet layout (full-width, pinned near the viewport bottom under the sm breakpoint) intentionally keeps the same fade+scale rather than a directional slide-from-bottom, per the component plan's call to reuse one motion signature rather than add a second pattern for one breakpoint. Highlighted-row background change on CommandMenuItem uses --pd-duration-instant with --pd-ease-standard, matching Button's press-feedback timing, because it is state feedback rather than decoration. Under prefers-reduced-motion, both the panel/scrim transition and the row-highlight transition are removed via motion-reduce:transition-none (scale pinned to 100% via motion-reduce:scale-100), leaving the opacity and background-colour changes themselves intact so every state change stays legible without motion.",
    "schema": {
      "component": "CommandMenu",
      "props": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "CommandMenu",
        "type": "object",
        "properties": {
          "open": {
            "type": "boolean",
            "description": "Controlled open state. There is no uncontrolled fallback — see engineeringNotes."
          },
          "label": {
            "type": "string",
            "description": "Accessible dialog name. Not visible; announced once when the menu opens."
          }
        },
        "required": [
          "open"
        ]
      },
      "events": [
        {
          "name": "onOpenChange",
          "required": true,
          "params": [
            {
              "name": "open",
              "type": "boolean"
            }
          ]
        }
      ]
    },
    "subcomponents": [
      {
        "component": "CommandMenuInput",
        "props": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "title": "CommandMenuInput",
          "type": "object",
          "properties": {}
        },
        "events": []
      },
      {
        "component": "CommandMenuList",
        "props": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "title": "CommandMenuList",
          "type": "object",
          "properties": {}
        },
        "events": []
      },
      {
        "component": "CommandMenuEmpty",
        "props": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "title": "CommandMenuEmpty",
          "type": "object",
          "properties": {}
        },
        "events": []
      },
      {
        "component": "CommandMenuGroup",
        "props": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "title": "CommandMenuGroup",
          "type": "object",
          "properties": {}
        },
        "events": []
      },
      {
        "component": "CommandMenuItem",
        "props": {
          "$schema": "https://json-schema.org/draft/2020-12/schema",
          "title": "CommandMenuItem",
          "type": "object",
          "properties": {
            "icon": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "$ref": "#/$defs/UINode"
                },
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string"
                      },
                      {
                        "$ref": "#/$defs/UINode"
                      }
                    ]
                  }
                }
              ],
              "x-react": "ReactNode",
              "description": "Leading icon, muted by default so it recedes behind the label. size-4 recommended."
            },
            "keybind": {
              "type": "string",
              "description": "Rendered visibly in the row and, because it stays in the item's text flow rather\nthan an aria-hidden decoration, announced as part of the option's accessible name —\nsatisfying \"hotkeys are both visible and announced\" without extra ARIA wiring."
            }
          },
          "$defs": {
            "UINode": {
              "type": "object",
              "description": "A nested component: a name from this catalogue, and the props to give it.",
              "required": [
                "component"
              ],
              "properties": {
                "component": {
                  "type": "string",
                  "description": "The component's exported name, e.g. \"MediaResult\"."
                },
                "props": {
                  "type": "object",
                  "description": "That component's own props, matching its entry in this catalogue."
                }
              }
            }
          }
        },
        "events": [
          {
            "name": "onSelect",
            "required": true,
            "params": [
              {
                "name": "value",
                "type": "string"
              }
            ]
          }
        ]
      }
    ]
  },
  "docs": "Requires `cn` at `@/lib/utils`. The host app owns the global ⌘K/Ctrl+K keydown listener and the `open` boolean — this component has no built-in trigger or shortcut binding, matching its controlled-only API. `CommandMenuItem`'s `keybind` prop is a single visible string (e.g. \"⌘S\"), not per-key badges; split it yourself if you need individually boxed keys.",
  "categories": [
    "actions",
    "overlays"
  ],
  "type": "registry:ui"
}